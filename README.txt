To run the project open the terminal in the linux download folder after downloading the related files to it and run the fallowing commands

chmod +x setup_environment.sh
./setup_environment.sh

you may need to insert the computer password during the file execution 
